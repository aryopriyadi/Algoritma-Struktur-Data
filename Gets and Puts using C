#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <windows.h>

void gotoxy(int x, int y){//fungsi goto untuk dipanggil ke main
    COORD coord;
    coord.X = x;
    coord.Y = y;
    SetConsoleCursorPosition(GetStdHandle(STD_OUTPUT_HANDLE),coord);
}

int main(){
  /*int panjang = 5, lebar = 4;
  float luas = panjang*lebar;
  printf("Luas adalah = %5.2f",luas);
  //%5.2f = 5 angka di depan koma, 2 angka di belakang koma, dengan tipe data float
  */

  /*char nama[50];
  puts("Input Nama : ");
  gets(nama);
  puts(nama);*/

  /*gets dan puts bersifat static
  gets hanya mampu menampung 1 statement
  puts juga hanya mampu menampilkan 1 statement
  */

  system("cls");
  float panjang, lebar, luas, keliling;
  gotoxy(30,10);printf("Masukkan panjang : ");//fungsi gotoxy yg sudah dipanggil
  scanf("%f", &panjang);
  gotoxy(30,11);printf("Masukkan lebar : ");
  scanf("%f", &lebar);
  luas = panjang*lebar;
  keliling = 2*panjang*lebar;
  gotoxy(30,13);printf("Luasnya adalah : %5.2f",luas);
  gotoxy(30,14);printf("Kelilingnya adalah : %5.2f\n",keliling);
}
